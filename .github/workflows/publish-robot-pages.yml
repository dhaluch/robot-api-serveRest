name: (deprecated) Publish Robot report to GitHub Pages

on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated. Use .github/workflows/robot-ci-pages.yml"
